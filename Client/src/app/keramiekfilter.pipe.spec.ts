import { KeramiekfilterPipe } from './keramiekfilter.pipe';

describe('KeramiekfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeramiekfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
